# Import environment with:
# conda env create -f environment.yml
# conda env update --prefix ./env --file environment.yml --prune

name: datascience
dependencies:
  - python
  - jupyter
  - matplotlib
  - numpy
  - pandas
  - scikit-learn
  - seaborn
  - geopandas
  - python-graphviz
  - xgboost
  - tensorflow
  - plotly
  - pip
  - pip:
      - keras
      - pydot

  